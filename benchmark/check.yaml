prelude: |-
  $LOAD_PATH.unshift(File.expand_path("lib"))
  require "strscan"
  scanner = StringScanner.new("test string")
benchmark:
  regexp_pattern: |
    scanner.check(/\w/)
  regexp_literal: |
    scanner.check(/test/)
  string: |
    scanner.check("test")
